require 'rails_helper'

RSpec.describe GerritOwner, type: :model do

  describe "GerritOwner" do

    it "is saved with correct parameters" do
      # gerrit_owner = create(:gerrit_owner)
      # expect(gerrit_owner).to be_valid
      # expect(gerrit_owner.account_id).to eq(1)
      # expect(GerritOwner.all.count).to eq(1)
    end


  end

end
